{
    "cluster": "sbatch --mem={cluster.mem} --partition={cluster.partition} -c 1 -n {cluster.n}  -t {cluster.time} -o {cluster.out} -e {cluster.err}",
    "cluster_config": "/home/zheng/test_parameters/snakemake/clusterconfig-2.json",
    "nodes": 100,
    "latency_wait": 300,
    "restart_times": 5
}
